// app/robots.ts
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: "*",
        allow: "/",

        // 🔒 Routes non indexables (privées / techniques)
        disallow: [
          "/auth/",
          "/dashboard/",
          "/tchat/",
          "/api/",
        ],
      },
    ],

    // 🗺️ Sitemap déclaré explicitement
    sitemap: "https://eleveai.fr/sitemap.xml",

    // 🌍 Host canonique (important pour Bing)
    host: "https://eleveai.fr",
  };
}
